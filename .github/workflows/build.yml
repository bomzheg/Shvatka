# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: ShvatkaBotBuild

on:
  push:
    branches: [ master ]
    tags: ["*.*.*"]

jobs:
  lint-and-test:
    uses: ./.github/workflows/test.yml
  build:
    needs: lint-and-test
    runs-on: ubuntu-latest
    env:
      VERSION_YAML: "vcs_hash: ${GITHUB_SHA}"

    steps:
      - uses: actions/checkout@v4
      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%Y-%m-%dT%H:%M:%S%z')"
      - name: debug
        env:
          VERSION_YAML: '{"vcs_hash": ${GITHUB_SHA}, "build_at": ${{ steps.date.outputs.date }} }'
        run: echo $VERSION_YAML
      - uses: mr-smithers-excellent/docker-build-push@v6.2
        env:
          VERSION_YAML:  '{"vcs_hash": ${GITHUB_SHA}, "build_at": ${{ steps.date.outputs.date }} }'
        with:
          image: bomzheg/shvatka
          addLatest: true
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
